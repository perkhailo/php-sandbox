# PHP Demo environment

This is a PHP demo application environment.

Once you have Docker and Docker Compose installed.

You can get this environment up and running with:

```
docker-compose up --build -d
```
